server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/crb96java/config-server-evn
management:
  endpoints:
    web:
      exposure:
        include: "*"
